import React from 'react';
import Child from './Child';

const Parent = ()=> {
    return(
        <div>
            <h2>this is in parent</h2>
            <Child />
        </div>
    )
}

export default Parent;